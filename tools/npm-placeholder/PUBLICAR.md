# Historical npm placeholder

This directory preserves the artifact published as `judg3d@0.0.1` on
2026-08-20. It is not part of the workspace and is not the usable implementation.
The historical tarball SHA-1 is `4098127e5d18bbcae56b9aeb637be1d63bbb2663`.

Do not publish a new release from this directory. The usable 0.2.0 source
comes from `packages/cli` and its workspace dependencies. Follow the current
[release checklist](../../docs/maintainers/release.md), including clean package
installation and an explicit owner decision before publishing.

The archived package metadata describes the old release; the current source
license and package metadata are defined at the repository root and in
`packages/*`. No change here retroactively replaces the historical npm artifact.
