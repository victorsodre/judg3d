# Fixtures

Fixtures are **immutable**. Do not edit a file here in place: a new case needs
a new file and name. An asset verdict is meaningful only if the tested asset
does not change underneath it.

## Valid — Khronos glTF-Sample-Assets

Downloaded from `KhronosGroup/glTF-Sample-Assets` at commit
`bf2bb4a81c73a7ceb53e80df3dec0105c5a3fdef`; sha256 was verified during download
by `scripts/fetch-khronos.mjs`.

| File | Source | Bytes | sha256 | License |
|------|--------|-------|--------|---------|
| `valido.glb` | `Models/Box/glTF-Binary/Box.glb` | 1664 | `ed52f7192b8311d700ac0ce80644e3852cd01537e4d62241b9acba023da3d54e` | CC BY 4.0, (c) 2017 Cesium |
| `valido-textura.glb` | `Models/BoxTextured/glTF-Binary/BoxTextured.glb` | 5956 | `b510eca2e2ef33f62f9ed57d6e7ce2d10ebb2bdebc4a8e59d347719ba81abdf4` | CC BY 4.0, (c) 2017 Cesium |

`valido-textura.glb` uses the Cesium logo as a texture; the attribution above
is required by CC BY 4.0. It exercises the validator resource and image path
that `valido.glb`, which has no texture, does not cover.

Both pass cleanly: zero errors, warnings, and information messages.

## Broken — generated

| File | Bytes | sha256 |
|------|-------|--------|
| `quebrado.glb` | 1612 | `641449b12c1f7f27c232c4586e4746374396f90fb5b085396671e5edcba96fe1` |

Generated by `scripts/make-quebrado.mjs` from `valido.glb`. The GLB container
remains well-formed; the defect is semantic so that the validator can reach the
content and return a readable violation:

| Defect | Expected code | Pointer |
|--------|---------------|---------|
| `attributes.POSITION` points to accessor 99 | `UNRESOLVED_REFERENCE` | `/meshes/0/primitives/0/attributes/POSITION` |
| `bufferViews[0]` has no `buffer` property | `UNDEFINED_PROPERTY` | `/bufferViews/0` |
| `primitives[0].mode` is a string | `TYPE_MISMATCH` | `/meshes/0/primitives/0/mode` |

The validator also emits an Information-severity `UNUSED_OBJECT`, which the
`web-commerce` profile filters (`report: "warn"`).

## Commands

```bash
pnpm fixtures:fetch          # re-download valid fixtures and verify sha256
node fixtures/scripts/fetch-khronos.mjs --check   # only verify files on disk
pnpm fixtures:broken         # regenerate quebrado.glb (deterministic, same sha256)
```
